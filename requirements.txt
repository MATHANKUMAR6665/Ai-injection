pandas>=2.0.0
matplotlib>=3.7.0
reportlab>=4.0.0

# Optional -- only needed if you switch --backend to a real model later
# openai>=1.0.0
# anthropic>=0.40.0

# Optional -- for running the automated tests
# pytest>=7.0.0
